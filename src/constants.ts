// TODO Turn these into functions so they can be called on game instantiation,
// or even updated with host's sendFullState. Nah don't do that that's obnoxious.
// export const WIDTH = 1000
// export const HEIGHT = WIDTH / 2
export const WIDTH = window.innerWidth - 5
export const HEIGHT = window.innerHeight - 5
